{
  "description": "maestro liaison enforcement. SessionStart matches startup|resume|clear|compact because the injected posture lives in context, not on disk — compaction would silently strip it mid-session, so it must be re-injected after every compact (full rationale in scripts/sessionstart-digest.sh header). PreToolUse warns, never denies. Stop blocks blind turn-ends within a bounded budget.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sessionstart-digest.sh\"",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Read|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pretool-warn.mjs\"",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/stop-guard.mjs\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
